export interface Product {
  id: string;
  name: string;
  price: number;
  image: string;
  category: string;
  rating: number;
  reviews: number;
  description: string;
  seller: string;
  stock: number;
}

export interface CartItem extends Product {
  quantity: number;
}
